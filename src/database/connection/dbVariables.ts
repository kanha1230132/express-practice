/**
 * Table Names 
 */
export enum tablesName {
  userTable = "USER_TABLE",
}

/**
 * User Table Columns
 */
export enum userTable {
  userId = "userId",
  firstName = "firstName",
  lastName = "lastName",
  email = "email",
  imageURL = "imageURL",
  createdBy = "createdBy",
  deleted = "deleted",
  deleteBy = "deleteBy",
  createdAt = "createdAt",
}
